{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --workspace @cads/cads",
    "dev:spheroscope": "npm run dev --workspace @cads/spheroscope",
    "build": "npm run build --workspace @cads/cads --workspace @cads/spheroscope",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "generate-client": "npm run generate-client --workspace shared",
    "generate-component": "npm run generate-component --workspace shared"
  },
  "workspaces": [
    "shared/",
    "cads/",
    "spheroscope/"
  ],
  "dependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8"
  }
}
